-- Confirmed With quedó como texto libre persistente en uld.confirmed_with.
-- La tabla uld_confirmed_source era para un dropdown que se descartó: se elimina.
DROP TABLE IF EXISTS uld_confirmed_source;