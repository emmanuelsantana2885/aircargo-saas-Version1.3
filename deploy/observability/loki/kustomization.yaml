apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: logging

resources:
- loki-stack.yaml

commonLabels:
  app: loki-stack
  component: observability

images:
- name: grafana/loki
  newTag: 2.9.0
- name: timberio/vector
  newTag: 0.38.0-alpine